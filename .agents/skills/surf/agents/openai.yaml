interface:
  display_name: "Surf"
  short_description: "Repo-specific patterns and workflows for surf"
  default_prompt: "Use the surf repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true